# Create a topic
./opt/kafka/bin/kafka-topics.sh --create \
  --zookeeper zookeeper:2181 \
  --replication-factor 1 --partitions 13 \
  --topic my-topic

# producer
./opt/kafka/bin/kafka-console-producer.sh \
    --broker-list 0.0.0.0:9092 \
    --topic usuarios-create

#Consumer
./opt/kafka/bin/kafka-console-consumer.sh \
    --bootstrap-server 0.0.0.0:9092 \
    --topic usuarios-create \
    --from-beginning


#Correr stack
kubectl apply -f  kafka-dep.yaml,kafka-svc.yaml,zookeeper-dep.yaml,zookeeper-svc.yaml,ingress-rules-tfm.yaml,ingress-controller-nginx-tfm.yaml,ingress-service-tfm.yaml,mg-deploy.yaml